@model IEnumerable<LtiConsumer>

@{
    ViewBag.Title = "LTI Tool Consumers";
}

<h2>@ViewBag.Title.</h2>
<h3>A list of all the LTI Tool Consumers registered in this provider.</h3>

<p>
    Remember, you are one of several users of this sample provider. 
    Please do not muck with other people's LTI consumers.
</p>

@if (!Model.Any()) {
    <text>
        No consumers have been registered yet.
    </text>
} else {
    <text>
        <table class="table">
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Key)
                </th>
                <th></th>
            </tr>

        @foreach (var item in Model) {
            <tr>
                <td>
                    @Html.ActionLink(item.Name, "ConsumerDetails", new { id=item.LtiConsumerId })
                </td>
                <td>
                    @item.Key
                </td>
                <td>
                    @Html.ActionLink("Edit", "ConsumerEdit", new { id=item.LtiConsumerId }) |
                    @Html.ActionLink("Delete", "ConsumerDelete", new { id=item.LtiConsumerId })
                </td>
            </tr>
        }

        </table>
    </text>
}        

<p>
    @Html.ActionLink("Create New", "ConsumerCreate") |
    @Html.ActionLink("Back to Home", "Index", "Admin")
</p>

